# Image2Trailer 발표 준비용 최종 정리

작성일: 2026-06-21

## 1. 주제 한 문장

**Image2Trailer는 이미지 한 장을 입력하면, 그 이미지를 영화의 한 장면처럼 해석하고 예고편 콘셉트, 포스터, 음성 내레이션까지 자동으로 만들어주는 AI Scene Director 데모이다.**

이 프로젝트의 핵심은 단순히 이미지를 생성하는 것이 아니라, 여러 AI 모델을 하나의 흐름으로 연결해 **이미지를 이해하고, 이야기를 만들고, 포스터와 목소리까지 완성하는 생성형 제작 파이프라인**을 만든 것이다.

## 2. 왜 이 주제를 선택했는가

이미지 생성이나 음성 합성은 각각 따로 보여주면 익숙한 데모가 된다. 하지만 실제 창작 과정에서는 한 장면을 보고 다음과 같은 의사결정이 연속적으로 일어난다.

- 이 장면에는 무엇이 보이는가?
- 어떤 분위기와 장르로 해석할 수 있는가?
- 어떤 제목과 로그라인이 어울리는가?
- 포스터는 어떤 시각 스타일이어야 하는가?
- 예고편 내레이션은 어떤 목소리로 읽혀야 하는가?

Image2Trailer는 이 과정을 하나의 데모로 묶었다. 그래서 발표에서는 “여러 모델을 단순히 나열한 것”이 아니라, **장면을 해석해서 콘텐츠 패키지로 확장하는 AI 제작 워크플로**를 구현했다는 점을 강조하면 된다.

## 3. 데모에서 사용자가 보는 흐름

사용자는 복잡한 설정을 몰라도 된다. 화면에서 선택하는 것은 네 가지다.

| 항목 | 발표 때 설명할 의미 |
| --- | --- |
| 원본 이미지 | 예고편의 출발점이 되는 한 장면 |
| 포스터 레퍼런스 | 포스터가 참고할 색감과 분위기 힌트 |
| 출력 언어 | 한국어 또는 영어 결과 선택 |
| 음성 모드 | 내 목소리 클론 또는 기본 목소리 선택 |

버튼을 누르면 결과 화면에는 다음이 나온다.

- 이미지에서 탐지한 주요 시각 단서
- 장면 분석
- 영화 장르
- 영화 제목
- 로그라인
- 예고편 대본
- 생성된 영화 포스터
- 음성 내레이션
- 각 단계가 성공했는지 보여주는 상태 정보

발표에서는 이 흐름을 “한 장의 이미지가 영화 예고편 패키지로 확장된다”고 설명하면 된다.

## 4. 전체 파이프라인을 쉽게 설명하기

발표용으로는 다음 네 단계로 설명하면 가장 이해하기 쉽다.

### 4.1 먼저, 이미지를 관찰한다

첫 번째 모델은 이미지 안에서 중요한 단서를 찾는다. 사람, 얼굴, 도로, 창문, 빛, 그림자 같은 요소를 탐지해서 다음 단계가 장면을 해석할 근거를 만든다.

발표 멘트 예시:

> 첫 단계에서는 이미지 안의 시각 단서를 찾아냅니다. 사람이 있는지, 도로인지, 빛이나 그림자가 있는지 같은 정보를 뽑아서 이후 스토리 생성의 근거로 사용합니다.

### 4.2 다음으로, 장면을 영화적으로 해석한다

두 번째 모델은 이미지를 보고 영화 감독처럼 장면을 해석한다. 이때 단순 설명이 아니라 영화 콘셉트로 바꾼다.

생성되는 내용은 다음과 같다.

- 장면의 분위기
- 장르
- 제목
- 로그라인
- 예고편 대본
- 포스터 생성에 필요한 설명
- 음성으로 읽을 내레이션 문장

발표 멘트 예시:

> 여기서는 이미지를 단순히 설명하지 않고, 영화의 한 장면으로 해석합니다. 그래서 장르, 제목, 로그라인, 예고편 대본까지 구조화해서 만들어냅니다.

### 4.3 그다음, 포스터를 만든다

세 번째 모델은 앞에서 만든 영화 콘셉트를 바탕으로 포스터를 생성한다. 중요한 점은 포스터 위의 글자를 나중에 코드로 붙인 것이 아니라, 이미지 생성 모델이 제목까지 포함한 포스터를 직접 만들도록 했다는 것이다.

발표 멘트 예시:

> 포스터는 단순 배경 이미지가 아니라 영화 포스터처럼 제목까지 포함해서 생성하도록 만들었습니다. 텍스트를 따로 덧붙인 것이 아니라 이미지 생성 모델이 포스터 자체를 만들게 했습니다.

### 4.4 마지막으로, 예고편 내레이션을 만든다

네 번째 모델은 대본을 음성으로 읽는다. 여기서 두 가지 모드를 지원한다.

- 내 목소리 클론
- 기본 목소리

내 목소리 클론은 미리 녹음한 약 20초 분량의 목소리를 참고해서 예고편 내레이션을 생성한다. 또한 한국어 모드와 영어 모드를 선택할 수 있다.

발표 멘트 예시:

> 마지막 단계에서는 예고편 대본을 음성으로 합성합니다. 특히 제 목소리를 미리 녹음해두고, 그 목소리 스타일로 한국어 또는 영어 내레이션을 생성할 수 있게 했습니다.

## 5. 완성된 핵심 기능

현재 데모에서 완성된 기능은 다음과 같다.

| 기능 | 발표 때 강조할 포인트 |
| --- | --- |
| 이미지 기반 장면 해석 | 이미지 한 장을 영화 콘셉트로 변환 |
| 객체 탐지 | 스토리 생성의 시각적 근거 제공 |
| 영화 제목/장르/로그라인 생성 | 단순 설명이 아니라 창작 결과 생성 |
| 예고편 대본 생성 | 발표에서 바로 읽히는 narration script 생성 |
| 포스터 생성 | 제목 포함 영화 포스터 직접 생성 |
| 한국어/영어 모드 | 같은 이미지로 언어별 결과 생성 가능 |
| 내 목소리 클론 | 개인화된 예고편 내레이션 생성 |
| 기본 목소리 선택 | voice clone 없이도 안정적으로 음성 생성 가능 |
| 단계별 fallback | 발표 중 한 단계가 실패해도 UI 흐름 유지 |

## 6. 모델들이 맡은 역할을 자세히 설명하기

발표에서는 모델 이름보다 **각 모델이 어떤 입력을 받고, 무엇을 판단하고, 다음 단계에 무엇을 넘기는지**를 설명하는 것이 좋다. 이 프로젝트의 핵심은 모델 네 개가 독립적으로 실행되는 것이 아니라, 앞 단계의 결과가 다음 단계의 창작 재료가 된다는 점이다.

```text
원본 이미지
  + 사용자가 입력한 탐지 쿼리
  -> Grounding DINO
  -> 탐지된 객체 이름, confidence, 위치 정보
  -> Gemma Director
  -> 영화 콘셉트, 제목, 대본, 포스터 프롬프트, TTS 문장
  -> Z-Image-Turbo / Qwen3-TTS
  -> 포스터와 음성 내레이션
```

### 6.1 Grounding DINO: 이미지를 관찰하는 역할

Grounding DINO는 이미지 안에서 사용자가 지정한 탐지 쿼리에 해당하는 대상을 찾는다. 예를 들어 사용자가 다음처럼 쿼리를 넣을 수 있다.

```text
person. flower. butterfly. building. light.
```

그러면 Grounding DINO는 이미지 전체에서 이 단어들과 관련된 영역을 찾고, 각 객체에 대해 세 가지 정보를 만든다.

- 객체 이름
- confidence, 즉 얼마나 확실한지
- 이미지 안에서의 위치 box

예시는 다음과 같다.

```text
- person (0.91) at box (120, 400, 820, 1500)
- butterfly (0.78) at box (300, 80, 520, 260)
- flower (0.73) at box (50, 20, 1200, 700)
```

이 정보는 두 가지로 사용된다.

첫째, 화면에는 박스가 그려진 이미지로 표시된다. 그래서 발표자가 “어떤 쿼리 객체를 찾았는지”를 시각적으로 보여줄 수 있다.

둘째, 같은 정보가 다음 단계인 Gemma Director에게 전달된다. 이때 단순히 “사람이 있다” 정도가 아니라, “사람이 어느 위치에 있고, 꽃이나 나비가 어느 위치에 있는지”까지 함께 전달된다.

발표 멘트 예시:

> 첫 번째 모델은 사용자가 지정한 쿼리를 기준으로 이미지 안의 단서를 찾습니다. 예를 들어 사람, 꽃, 나비를 찾으라고 하면 해당 객체의 이름, 신뢰도, 위치를 뽑아냅니다. 이 결과는 단순히 화면에 박스를 그리는 데만 쓰는 것이 아니라, 다음 단계의 장면 해석 모델에게 구조화된 힌트로 전달됩니다.

### 6.2 Gemma 4: 장면을 영화적으로 해석하는 역할

Gemma는 이 프로젝트에서 디렉터 역할을 맡는다. 여기서 중요한 점은 Gemma가 Grounding DINO의 결과만 읽는 것이 아니라, **원본 이미지와 탐지 결과를 함께 본다**는 것이다.

Gemma가 받는 입력은 다음과 같다.

```text
원본 이미지
+ 탐지된 객체 이름
+ confidence
+ box 위치
+ 출력 언어 모드
```

예를 들어 꽃과 나비가 위쪽에 크게 있고, 사람들이 아래쪽에 모여 있다면 Gemma는 이를 단순히 “사람과 꽃이 있음”으로 끝내지 않는다. 이미지의 구도와 표정, 색감, 탐지된 객체 정보를 함께 보고 영화적 의미로 바꾼다.

예시는 다음과 같다.

```text
DINO가 넘긴 단서:
- person: 아래쪽에 여러 명
- flower: 화면 위쪽에 크게 분포
- butterfly: 꽃 장식 주변에 위치

Gemma의 해석:
친구들이 거대한 꽃과 나비 아래에서 웃고 있는 환상적인 여름의 순간.
장르는 청춘 판타지 또는 판타지 로맨스로 해석 가능.
```

Gemma가 최종적으로 만드는 것은 다음과 같다.

- 장면 분석
- 장르
- 영화 제목
- 로그라인
- 예고편 대본
- 포스터 생성을 위한 설명
- 음성으로 읽을 내레이션 문장

또한 사용자가 한국어 모드를 선택하면 이 결과를 한국어로 만들고, 영어 모드를 선택하면 영어로 만든다. 다만 포스터 생성 모델이 이해하기 쉽도록 포스터 프롬프트는 영어로 유지한다.

발표 멘트 예시:

> 두 번째 모델은 디렉터 역할입니다. 원본 이미지만 보는 것이 아니라, 앞 단계에서 탐지된 객체 이름과 위치 정보까지 같이 받아서 장면을 영화적으로 해석합니다. 그래서 단순 설명이 아니라 장르, 제목, 로그라인, 예고편 대본, 포스터 프롬프트, 음성 내레이션 문장을 만들어냅니다.

### 6.3 Z-Image-Turbo: 영화 포스터를 만드는 역할

Z-Image-Turbo는 Gemma가 만든 영화 콘셉트를 시각 결과물로 바꾸는 역할을 한다. 이 단계에서 중요한 입력은 다음과 같다.

```text
영화 제목
+ 장르
+ Gemma가 만든 포스터 설명
+ 선택한 포스터 레퍼런스의 색감 힌트
```

초기 버전에서는 이미지 생성 후 제목이나 문구를 코드로 덧붙이는 방식도 가능했다. 하지만 최종 버전에서는 그 방식을 제거했다. 지금은 Z-Image-Turbo가 영화 제목까지 포함한 포스터를 직접 생성하도록 했다.

이 설계의 의미는 다음과 같다.

- 결과물이 단순 배경 이미지가 아니라 포스터처럼 보인다.
- 제목 텍스트가 이미지 생성 과정 안에 포함된다.
- 사용자가 넣은 포스터 레퍼런스는 직접 복사되는 것이 아니라 색감과 분위기 힌트로 사용된다.

단, 이미지 생성 모델이 텍스트를 직접 만들기 때문에 제목 철자가 가끔 흔들릴 수 있다. 이 점은 남은 품질 리스크로 설명하면 된다.

발표 멘트 예시:

> 세 번째 모델은 포스터를 생성합니다. Gemma가 만든 제목과 포스터 설명을 받아서 영화 포스터 이미지를 만듭니다. 최종 버전에서는 제목을 나중에 덧붙이지 않고, 이미지 생성 모델이 포스터 안에 제목까지 직접 만들도록 했습니다.

### 6.4 Qwen3-TTS: 예고편 내레이션을 음성으로 바꾸는 역할

Qwen3-TTS는 Gemma가 만든 예고편 대본을 실제 음성으로 읽는 역할을 한다. 이 단계에서 받는 입력은 다음과 같다.

```text
Gemma가 만든 tts_text
+ 출력 언어 모드
+ 음성 모드
+ voice clone reference 오디오와 대본, 필요한 경우
```

음성 모드는 두 가지다.

| 음성 모드 | 의미 |
| --- | --- |
| 내 목소리 클론 | 미리 녹음한 약 20초 목소리를 참고해서 내레이션 생성 |
| 기본 목소리 | voice clone 없이 준비된 기본 화자로 내레이션 생성 |

한국어 모드에서는 한국어 대본을 한국어 음성으로 읽고, 영어 모드에서는 영어 대본을 영어 음성으로 읽는다.

내 목소리 클론은 다음 두 가지 reference를 사용한다.

- 미리 녹음한 목소리 샘플
- 그 샘플에서 실제로 읽은 문장

이 reference 덕분에 단순 TTS가 아니라 개인화된 예고편 내레이션처럼 들리게 만들 수 있다.

발표 멘트 예시:

> 마지막 모델은 예고편 대본을 음성으로 바꿉니다. 여기서는 기본 목소리도 사용할 수 있고, 제가 미리 녹음한 목소리를 reference로 사용해서 제 목소리 스타일로 내레이션을 만들 수도 있습니다. 언어 모드와 연결되어 있어서 한국어 모드에서는 한국어 음성, 영어 모드에서는 영어 음성으로 합성됩니다.

### 6.5 네 모델이 함께 만드는 최종 결과

각 모델의 역할을 한 문장씩 줄이면 다음과 같다.

| 단계 | 역할 | 다음 단계에 넘기는 것 |
| --- | --- | --- |
| Grounding DINO | 이미지에서 쿼리 객체를 찾음 | 객체 이름, confidence, 위치 |
| Gemma 4 | 장면을 영화적으로 해석함 | 제목, 장르, 로그라인, 대본, 포스터 설명, TTS 문장 |
| Z-Image-Turbo | 포스터를 생성함 | 영화 포스터 이미지 |
| Qwen3-TTS | 대본을 음성으로 읽음 | WAV 음성 파일 |

발표에서 가장 중요한 표현은 이것이다.

> DINO는 무엇이 어디에 있는지 찾고, Gemma는 그것을 영화적 의미로 해석하고, Z-Image는 그 해석을 포스터로 만들고, Qwen3-TTS는 그 이야기를 목소리로 들려줍니다.

## 7. 발표에서 꼭 강조할 차별점

### 7.1 단일 이미지에서 전체 콘텐츠 패키지 생성

입력은 이미지 한 장이지만 출력은 훨씬 풍부하다. 텍스트, 포스터, 음성까지 나온다.


### 7.2 언어와 목소리를 사용자가 선택 가능

한국어/영어 모드와 내 목소리/기본 목소리 모드를 조합할 수 있다. 발표에서는 이 부분이 사용자 경험 측면에서 강한 포인트다.

## 8. 5분 발표 대본 초안

### 시작

> 제가 만든 프로젝트는 Image2Trailer입니다. 이미지 한 장을 입력하면, 그 이미지를 영화의 한 장면처럼 해석하고, 영화 제목과 로그라인, 예고편 대본, 포스터, 그리고 음성 내레이션까지 생성하는 AI Scene Director 데모입니다.

### 사용 흐름

> 사용자는 원본 이미지를 올리고, 필요하면 포스터 레퍼런스를 선택합니다. 그리고 출력 언어를 한국어 또는 영어로 고르고, 음성은 제 목소리 클론 또는 기본 목소리 중에서 선택할 수 있습니다.

### 파이프라인 설명

> 내부적으로는 네 단계로 동작합니다. 먼저 사용자가 지정한 쿼리를 기준으로 이미지 안의 객체와 위치를 찾습니다. 그 결과는 단순히 박스 이미지로만 쓰이지 않고, 객체 이름과 confidence, 위치 정보로 정리되어 디렉터 모델에게 넘어갑니다. 디렉터 모델은 원본 이미지와 이 단서를 함께 보고 장면을 영화적으로 해석해서 제목, 장르, 로그라인, 대본을 만듭니다. 이후 이미지 생성 모델이 그 콘셉트를 바탕으로 제목이 포함된 영화 포스터를 생성하고, 마지막으로 TTS 모델이 예고편 대본을 선택한 목소리로 읽습니다.

### 완성 포인트

> 특히 이번 데모에서는 두 가지 선택 기능을 넣었습니다. 하나는 한국어와 영어 모드를 선택할 수 있다는 점이고, 다른 하나는 기본 목소리뿐 아니라 제 목소리를 미리 녹음해둔 샘플로 voice clone을 할 수 있다는 점입니다.


## 10. 예상 질문과 답변

### Q. 포스터의 제목은 나중에 붙인 것인가?

A. 최종 버전에서는 아니다. 초기에는 후처리로 텍스트를 얹는 방식도 있었지만 제거했고, 지금은 이미지 생성 모델이 제목을 포함한 포스터를 직접 만들도록 했다.

### Q. 내 목소리는 어떻게 만든 것인가?

A. 약 20초 정도의 목소리 샘플과 그 샘플에서 읽은 문장을 미리 저장해두고, TTS 모델이 이를 reference로 사용해 voice clone을 수행한다.

### Q. 한국어와 영어는 무엇이 바뀌는가?

A. 장면 분석, 제목, 로그라인, 대본, 음성 합성 언어가 선택한 모드에 맞게 바뀐다. 포스터 생성을 위한 내부 프롬프트는 안정성을 위해 영어를 유지하되, 제목 텍스트는 선택 언어를 반영하도록 했다.

### Q. 발표 중 한 단계가 실패하면 어떻게 되는가?

A. 각 단계는 상태 정보를 남기고 fallback을 갖도록 설계했다. 그래서 실패가 발생해도 전체 화면이 멈추는 것이 아니라 어떤 단계에서 문제가 생겼는지 확인할 수 있다.

## 11. 발표에서 피해야 할 설명

발표 시간은 5분이므로 다음 내용은 길게 설명하지 않는 것이 좋다.

- 내부 파일명
- 함수명
- 세부 클래스 구조
- 설치 과정
- 캐시 위치
- 가상환경 생성 과정
- 긴 실행 명령어
- 디버깅 과정 전체

대신 다음 세 가지에 집중한다.

1. 이미지 한 장이 어떻게 영화 예고편 패키지로 바뀌는가
2. 각 AI 모델이 어떤 역할을 맡는가
3. 최종 데모에서 사용자가 어떤 선택을 할 수 있는가

## 12. 최종 마무리 문장

**Image2Trailer는 이미지 이해, 스토리 생성, 포스터 생성, 음성 합성을 하나로 연결해, 한 장의 이미지를 개인화된 영화 예고편 패키지로 확장하는 AI 제작 데모이다.**
